# FileName={{proxy}}
[Unit]
Description=Proxy Caddy container

[Container]
Image=docker.io/library/caddy:latest
AutoUpdate=registry
AddCapability=CAP_NET_BIND_SERVICE
Volume=%h/.config/containers/systemd/proxy/config:/etc/caddy:rw,Z,U
Volume={{proxy}}-config.volume:/config:rw,Z,U
Volume={{proxy}}-data.volume:/data:rw,Z,U
Network={{proxy}}.network
PublishPort=80:80
PublishPort=443:443
PublishPort=443:443/udp

[Service]
Restart=always
RestartSec=5
TimeoutStartSec=300
TimeoutStopSec=30

[Install]
WantedBy=default.target
---
# FileName={{proxy}}-config
[Volume]
Label={{proxy}}-config
VolumeName=systemd-{{proxy}}-config
---
# FileName={{proxy}}-data
[Volume]
Label={{proxy}}-data
VolumeName=systemd-{{proxy}}-data
---
# FileName={{proxy}}
[Network]
Label={{proxy}}
