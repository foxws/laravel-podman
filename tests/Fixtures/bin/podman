#!/usr/bin/env bash

exit_code="${PODMAN_TEST_EXIT_CODE:-0}"

if [ "$exit_code" -ne 0 ]; then
    echo "podman: simulated failure (exit ${exit_code})" >&2
else
    echo "podman: $*"
fi

exit "$exit_code"
